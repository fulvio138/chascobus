import { SearchByDniPipe } from './search-by-dni.pipe';

describe('SearchByDniPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByDniPipe();
    expect(pipe).toBeTruthy();
  });
});
